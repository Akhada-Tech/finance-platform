import type { Migration } from './types';

/**
 * Ordered schema migrations.
 * No business tables yet — feature migrations are added here when implemented.
 */
export const migrations: Migration[] = [];
