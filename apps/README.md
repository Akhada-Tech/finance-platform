# apps/

## Why it exists

Holds deployable applications. Today only `mobile` exists.

## What belongs here

- Runnable apps (`mobile`, future `backend`, `website`)

## What must never go here

- Shared libraries (those live in `packages/`)
- Architecture docs (those live in `docs/`)
