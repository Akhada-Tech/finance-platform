{
  "name": "@finance-platform/eslint-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared ESLint configs for the finance-platform monorepo",
  "files": [
    "base.js",
    "react-native.js"
  ],
  "exports": {
    "./base": "./base.js",
    "./react-native": "./react-native.js"
  },
  "scripts": {
    "lint": "echo \"Config package — no source to lint\"",
    "typecheck": "echo \"Config package — no types to check\"",
    "test": "echo \"Config package — no tests\""
  },
  "peerDependencies": {
    "eslint": ">=8",
    "@react-native/eslint-config": "*"
  },
  "peerDependenciesMeta": {
    "@react-native/eslint-config": {
      "optional": true
    }
  }
}
